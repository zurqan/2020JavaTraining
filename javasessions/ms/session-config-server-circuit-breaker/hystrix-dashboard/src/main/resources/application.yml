server:
  port: 4000

spring:
  application:
    name: hystrix-dashboard

eureka:
  instance:
    prefer-ip-address: true


hystrix:
  dashboard:
    proxy-stream-allow-list:
      - "*"
management:
  endpoints:
    web:
      exposure:
        include:
          - "*"